/* Primeirao de todos arquivao em linguagem C eba uhul
Vamos importar a biblioteca base para executar o nosso programa.
a biblioteca se chama stdio (std = standard, | i = input | o = output)
para importar, usaremos o comando #include */

#include <stdio.h>

/*Abaixo iremos escrever os comandos que serão realmente executados
utlizaremos a função main (principal) para iniciar a execução*/

int main(){
    // o comando printf exibe uma mensagem na tela do console
    printf ("Hello, World! \n");
    // informa ao compilador (tradutor) que o programa acabou
    return 0;
}